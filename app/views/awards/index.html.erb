<h1>Admin - Add A New Award</h1>

<% if current_admin? %>
  <%= form_for [@admin, @award] do |f|  %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.submit "Submit" %>
  <% end %>
<% end %>
